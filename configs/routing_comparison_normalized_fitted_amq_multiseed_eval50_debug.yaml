experiment:
  name: routing_comparison_normalized_fitted_amq_multiseed_eval50_debug
  output_dir: results

base_config: configs/routing_comparison_normalized_fitted_amq_eval50_debug.yaml
seeds: [0, 1, 2]
